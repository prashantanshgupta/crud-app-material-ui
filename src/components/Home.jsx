const Home = () => {
  return (
    <div>
      <h1>CRUD Application</h1>
      <p>
        This Application is about crud application and built using
        React,Material UI, React Router Dom, JSON Server
      </p>
    </div>
  );
};

export default Home;
